{
  "devDependencies": {
    "aws-sdk": "^2.1300.0",
    "express": "^4.18.2",
    "lambda-local": "^2.0.3",
    "nodemon": "^2.0.20",
    "vitest": "^0.28.1"
  },
  "scripts": {
    "dev": "nodemon -L dev-server.js",
    "test": "vitest run",
    "test-watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
